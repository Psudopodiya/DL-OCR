import React from 'react'

const CroppedData = ({canvas}) => {
    const startX = 12
    const startY = 12
    const width = 500
    const heigth = 500
  return (
    <div>

    </div>
  )
}

export default CroppedData